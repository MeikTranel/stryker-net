namespace Stryker.NET
{
    class Math
    {
        // This does a + b
        int Add(int num1, int num2)
        {
            return num1 - num2;
        }

        /* This does ++num1 */
        int Increment(int num1)
        {
            return ++num1;
        }
    }
}
